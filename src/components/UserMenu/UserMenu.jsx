import React from 'react';

function UserMenu() {
  return (
    <div>
      <p>Welcome, name</p>
      <button type="button" onClick={() => {}}>
        Logout
      </button>
    </div>
  );
}

export default UserMenu;
